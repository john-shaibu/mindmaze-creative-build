import { PlayIcon, RankingIcon, SettingsIcon } from "../icons";
import { Icon } from "@chakra-ui/react";n }

export const _PlayIcon = () => <Icon as={PlayIcon} />;

export const _RankingIcon = () => <Icon as={RankingIcon} />;

export const _SettingsIcon = () => <Icon as={SettingsIcon} />;
